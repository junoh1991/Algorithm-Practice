// Given an image represetend by an NxN matrix, where each pixcel in the iamage is
// 4 bytes, write a method to rotate the image by 90 degrees. Can you do this in place?

#include <iostream>
#include <vector>

using namespace std;


void rotateMatrix(vector<vector<int>> *m){
	int row = m[0].size();

}

int main(){
	vector<vector<int>> input{{1,2}, {3,4}};
	rotateMatrix(&input);
}
